// The GLM profile currently mirrors the anthropic profile exactly.
// Fork the anthropic files here when GLM-specific tuning diverges.
export { buildSystemPrompt } from "../../anthropic/system/index.tsx";
