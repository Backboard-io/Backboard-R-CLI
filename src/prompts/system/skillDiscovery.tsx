export const SKILL_DISCOVERY_GUIDANCE = `## Skill & MCP discovery

Skill and MCP discovery are enabled for this session.

- Before starting a specialized or non-trivial task — building a UI, generating a document, scaffolding a project, a multi-step workflow — first call \`find_skill\` with a short description of the task. A project or community skill may encode how to do it well; if a returned skill fits, use its instructions instead of improvising.
- If the task needs an integration you have no tool for (a database, an API, a SaaS product), call \`find_mcp\` to add a server that provides the tools.
- Prefer discovering and using a relevant skill over doing specialized work unaided. Skip discovery only for trivial edits, or when a relevant skill is already loaded.`;
